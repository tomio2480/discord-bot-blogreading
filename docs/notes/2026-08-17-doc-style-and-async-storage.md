# ドキュメント体裁の統一と，ストレージ呼び出しの非同期化

2026-08-17 のセッションで扱った 4 件の作業と，そこで得た判断の記録である．
残していた知見 PR のマージ，利用者向け文書の体裁統一，テストのモック解除，
Sheets 呼び出しの非同期化を扱った．

## 📚 目次

- [Codex の P1 指摘をコード修正で受けなかった理由](#-codex-の-p1-指摘をコード修正で受けなかった理由)
- [文体は場所ではなく読み手で決める](#-文体は場所ではなく読み手で決める)
- [体裁の修正が内容を書き換えることがある](#-体裁の修正が内容を書き換えることがある)
- [テストのモックは依存の版に紐づく](#-テストのモックは依存の版に紐づく)
- [同期処理をイベントループから逃がす](#-同期処理をイベントループから逃がす)
- [作業環境の学び](#-作業環境の学び)
- [残課題](#-残課題)

## 🔍 Codex の P1 指摘をコード修正で受けなかった理由

PR #16 に対し Codex は次を指摘した．
Sheets 障害かつキャッシュ無しの状況でも，18:30 の `post_start()` は
`None` を `save_data()` へ渡す．`_unsynced` 経由で Sheets の値を消しうる．
`CLAUDE.md` に書いた「呼び出し側は既定値で上書き保存しない」と矛盾する，という内容である．

指摘の事実関係は正しい．ただし 18:30 の消去は 19:00 の新規作成に備えた設計であり，
仕様上の意図的な動作である．そこでコードは変えず，文書側の不変条件に例外を明記した．

判断の分かれ目は「その動作が仕様か，実装の漏れか」である．
Bot レビューは実装の一貫性を見るため，仕様上の例外を漏れとして報告する．
仕様を持つ側が例外を文書化すれば，同じ指摘は再発しない．

## 🧭 文体は場所ではなく読み手で決める

`README.md` と `GOOGLE_SHEETS_SETUP.md` は，ですます調のまま残した．
textlint の既定である「である調」には合わせていない．
どちらも利用者が手順どおりに操作するための文書である．
読み手へ呼びかける性質を持つためである．

textlint の指摘は 1 ルールだけ無効化して止めた．
対象は `ja-technical-writing/no-mix-dearu-desumasu` である．
無効化はファイル先頭のコメントで宣言する．`.textlintignore` は使わない．
ファイルごと除外すると，文長・助詞重複・表記ゆれの検査まで効かなくなる．

選んだ文体は `CLAUDE.md` に 1 行残した．後から来た者が同じ判断を繰り返さずに済む．

## ✂️ 体裁の修正が内容を書き換えることがある

句読点とスペースの統一を委譲した結果，助詞の重複を避ける言い換えの過程で
内容の変わった箇所が 5 件あった．表 1 に示す．

表 1. 体裁の修正に紛れて内容が変わった箇所

| 箇所 | 変更後 | 問題 |
|---|---|---|
| README 機能一覧 | 次の月曜日 → 次週月曜日 | 日曜に実行した場合の指す日が変わる |
| README 手順 3 | Discord Developer Portal → Developer Portal | リンク先のサービス名が消えた |
| README 手順 6 | 推奨します → 推奨されます | 行為者が消えた受動態 |
| SETUP 診断節 | ログ文言を独自に整形 | 実際の出力と一致せず検索できない |
| SETUP 動作説明 | 最後に保存した → 最後の保存済み | 意味は同じだが日本語として不自然 |

いずれも textlint は通る．機械検査は体裁しか見ないため，
内容の保存は差分の目視でしか担保できない．

対策は 2 つある．委譲時に「内容を変えずに解消できない箇所は据え置き，
報告に列挙する」と明示すること，そして受領時に差分を必ず読むことである．
今回のエージェントは据え置き判断を 3 件報告しており，仕組み自体は機能していた．
報告に挙がらなかった 5 件が，言い換えを体裁の範囲と見なした結果である．

ログ文言や UI ラベルの引用は，そもそも書き換えの対象から外す．
バッククォートで囲めば textlint の検査対象からも外れ，実物との一致も保てる．

## 🧪 テストのモックは依存の版に紐づく

`tests/test_bot.py` は `sys.modules['discord']` を `MagicMock` へ差し替えていた．
このためスラッシュコマンドのテスト 4 件が skip のまま残っていた．
discord.py 2.7.1 では差し替えなしで import できるため，モックを外して有効化した．

有効化では注意すべき点がある．
デコレータ適用後の関数は `app_commands.Command` になる．
`await bot.ls(interaction)` は `TypeError` になる．
元のコルーチンは `bot.ls.callback(interaction)` で呼ぶ．

旧テストは `response.send_message` を検証していた．
現行実装は 3 秒制限対策で `response.defer()` → `followup.send()` の順に呼ぶ．
skip されたテストは実装の変化に追随しない．
有効化の際は検証対象そのものを見直す．

## ⏱ 同期処理をイベントループから逃がす

Sheets の再試行は同期の `time.sleep` で待つ．最大 3 回・2 秒間隔のため，
障害時は最大 4 秒ほどイベントループが止まる．ハートビートと他コマンドが巻き込まれる．

`load_data` / `save_data` は同期のまま残した．
`asyncio.to_thread` で包む `aload_data` / `asave_data` を追加した．
置き換えたのは非同期側の呼び出しだけである．
同期版を残したのは，`load_data` が内部で `save_data` を呼ぶ再同期経路があるためである．
両方を非同期化すると，同期文脈から呼べなくなる．

既存テストは `bot.load_data` を `patch` している．`asyncio.to_thread(load_data)` は
呼び出し時にモジュールのグローバル名を解決するため，patch はそのまま効く．

## 🛠️ 作業環境の学び

- ローカルの `discord.py` は 2.3.2 で，`requirements.txt` の 2.7.1 と食い違っていた．
  検証はスクラッチパッドへ venv を作り，`requirements.txt` を入れて行う．
  システム環境を書き換えない．
- Python 3.13 では `audioop` が標準ライブラリから外れた．
  discord.py 2.3.2 は import に失敗する．2.7.1 は対応済みである．
- CI の完了待ちループは，チェックがまだ登録されていない時点で
  pending が無いと判定して即座に抜ける．
  チェック件数の下限も条件に入れる．
- 複数の独立タスクは worktree 分離のサブエージェントへ並行委譲できる．
  ただし同じファイルを触るタスクは分けない．今回は `CLAUDE.md` を 3 ブランチが
  それぞれ別の行で編集したため，マージ順にコンフリクトが出うる．

## 📌 残課題

- `CLAUDE.md` に残る textlint 指摘 7 件（Issue #23）．
- PR #21 → #22 の順にマージし，#22 の base を `main` へ切り替える必要がある．
- 3 ブランチが `CLAUDE.md` を編集しているため，マージ順によっては手で解消する．
- `create_hackmd_note()` の `requests` 呼び出しと RSS 取得は同期のままである．
  Sheets ほど待たないが，同じ理由でイベントループを止めうる．
